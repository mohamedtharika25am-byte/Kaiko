<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="all">

    <!-- 
        ========================================================================
        HARDWARE FEATURES
        ========================================================================
    -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <!-- 
        ========================================================================
        PERMISSIONS & RATIONALE
        ========================================================================
    -->
    <!-- SEND_SMS: Required to send emergency distress SMS to guardians -->
    <uses-permission
        android:name="android.permission.SEND_SMS"
        tools:ignore="ProtectedPermissions" />

    <!-- RECEIVE_SMS: Required to detect incoming guardian SOS acknowledgement SMS (v1.8.4) -->
    <uses-permission
        android:name="android.permission.RECEIVE_SMS"
        tools:ignore="ProtectedPermissions" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- POST_NOTIFICATIONS: Android 13+ status notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- VIBRATE: Tactile feedback on emergency trigger -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- READ_CONTACTS: Select emergency guardians from phone contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- RECORD_AUDIO: Hands-free Voice Trigger safety feature (v1.6.0) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- WAKE_LOCK: Lock-screen display and wake-up for assistant emergency triggers (v1.7.0) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Android 11+ Package Visibility Queries for Speech Recognition -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kaiko">

        <!-- 
            ====================================================================
            1. ONBOARDING & SETUP ACTIVITY
            ====================================================================
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dedicated User Guide screen -->
        <activity
            android:name=".UserGuideActivity"
            android:exported="false" />

        <!-- Dedicated Manage Guardians screen (v1.5.0) -->
        <activity
            android:name=".ManageGuardiansActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Dedicated Voice Trigger screen (v1.8.0) -->
        <activity
            android:name=".VoiceTriggerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Dedicated Settings screen (v1.8.0) -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Transparent activity to prompt for Location Services / GPS directly on current screen -->
        <activity
            android:name=".LocationPromptActivity"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:exported="false"
            android:taskAffinity=""
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop" />

        <!-- Dedicated Power Button Long Press SOS Confirmation Screen (v1.7.0) -->
        <activity
            android:name=".PowerButtonSosActivity"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 
            ====================================================================
            2. HARDWARE BUTTON LISTENER ACCESSIBILITY SERVICE
            ====================================================================
        -->
        <service
            android:name=".KaikoAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            tools:ignore="AccessibilityServicePolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 
            ====================================================================
            3. HOME SCREEN SOS WIDGET RECEIVER (DIRECT SOS)
            ====================================================================
        -->
        <receiver
            android:name=".KaikoWidgetProvider"
            android:exported="true"
            android:label="@string/widget_sos_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.yourname.kaiko.ACTION_TRIGGER_WIDGET" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- 
            ====================================================================
            4. DISGUISED HOME SCREEN WIDGET RECEIVER (DAILY MEMO)
            ====================================================================
        -->
        <receiver
            android:name=".KaikoDisguisedWidgetProvider"
            android:exported="true"
            android:label="@string/widget_disguised_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.yourname.kaiko.ACTION_TRIGGER_DISGUISED_WIDGET" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_disguised_info" />
        </receiver>

        <!-- 
            ====================================================================
            5. LOCK SCREEN QUICK SETTINGS TILE SERVICE
            ====================================================================
        -->
        <service
            android:name=".KaikoTileService"
            android:icon="@drawable/ic_sos"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 
            ====================================================================
            6. VOICE INTERACTION & DIGITAL ASSISTANT SERVICES (v1.7.0)
            ====================================================================
        -->
        <service
            android:name=".KaikoVoiceInteractionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true"
            android:label="@string/voice_interaction_service_label">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>

            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service_config" />
        </service>

        <service
            android:name=".KaikoVoiceInteractionSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true" />

        <service
            android:name=".KaikoRecognitionService"
            android:permission="android.permission.BIND_RECOGNITION_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

        <!-- 
            ====================================================================
            7. INTERNAL BROADCAST RECEIVERS FOR STATE & ESCALATION
            ====================================================================
        -->
        <receiver
            android:name=".EscalationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.yourname.kaiko.ACTION_ESCALATE_TIMER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".SmsSentReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.yourname.kaiko.ACTION_SMS_SENT" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.yourname.kaiko.ACTION_MARK_SAFE" />
                <action android:name="com.yourname.kaiko.ACTION_SIMULATE_ACK" />
            </intent-filter>
        </receiver>

        <!-- Guardian Response / SMS ACK BroadcastReceiver (v1.8.4) -->
        <receiver
            android:name=".GuardianAckReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
